import java.util.*;

public class TDD {
    public static int add(String numbers) {
        if (numbers.isEmpty())
            return 0;
        return 0;
    }

    public static void main(String[] args) {
        int ans = add("");
        System.out.println(ans);
    }
}